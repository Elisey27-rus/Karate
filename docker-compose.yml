version: '3.9'

services:
  web:
    build: .
    command: gunicorn mysite.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/mysite
    ports:
      - "8000:8000"
    environment:
      DJANGO_SETTINGS_MODULE: mysite.settings
